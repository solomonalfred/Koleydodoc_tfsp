version: "3.9"

services:

  api:
    container_name: kaleydodoc_api
    image: kaleydodoc_api
    build:
      context: ./
      dockerfile: "Docker/api/Dockerfile"
    ports:
      - "8000:8000"
    volumes:
      - ./files:/files
      - ./data:/data
      - ./src:/src
    restart: always
